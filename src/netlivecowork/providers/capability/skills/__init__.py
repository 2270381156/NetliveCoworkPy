"""Skill 能力 —— 本地目录里的 skill，与云端市场「引用式」加载的 skill。

**这个包里只有一个 provider**（``provider.py``）。其余都是它和 API 的支撑件——曾经它们
和它平铺在同一层，于是没人看得出谁是谁：

    provider.py     内核看见的那个 provider：列出可用 skill、用时才把 zip 取下来解压到
                    临时目录、跑完即删。**服务于模型**。

    adapters/       一家市场一个 adapter，只管"这一家怎么说话"（鉴权、翻页、字段改名）。
                    ``registry.py`` 是"这个部署有哪几家"的唯一来源。
                    上层不知道有几家、哪家要鉴权 —— 分法照搬 ``providers/llm/``。

    services/       用例层，**服务于人**：用户在技能页点添加/删除/上传走这里。
                    与 provider 共用引用库和 runtime，但入口不同、时机不同。

    references/     引用式加载的持久化。只记"引用了什么"，**不存内容**；内容归 runtime 管。

    runtime/        执行期机制：一串字节 → 能跑的目录 → 抹掉。不认识市场，也不认识引用库。

    legacy/         旧数据兼容（旧 pull 记录 → 引用）。带退休条件，不是永久居民。

    errors.py       错误类型与 HTTP 状态映射，全包共用。
    current_user.py 进程级「当前登录用户名」。运行时没有前端请求可问，只能从这里读。

对外不做再导出：谁用什么就从对应子包导入，免得这里变成一张什么都能拿到的大网，
让"哪一层依赖哪一层"重新变得看不见。
"""
