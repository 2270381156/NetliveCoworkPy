/**
 * 「地端 skill 集合变了」→ 同步到云端实例。**本分支是个空实现**。
 *
 * 云地协同不在本期范围（需求 §2.2）：地端就是唯一一份 skill，没有第二处要对齐。
 *
 * 保留这个模块而不是把 `api/skills` 里的调用删掉，理由同 `api/backends.ts`——
 * 触发点**故意**收在 API 这个咽喉处而不是各页面的 onSuccess 里：导入、删除、从市场
 * 引用、取消引用……入口不止一处，以后还会加，逐个记得调是迟早要漏的。删掉调用点，
 * 将来接云端时就要把这件事在每个入口重做一遍，而漏一处不报错，只是某次导入没同步过去。
 */

/** 触发一次同步（本分支无事可做）。永不抛错。 */
export function scheduleSkillSync(): void {}
